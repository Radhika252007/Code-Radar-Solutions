#include <stdio.h>

int bubbleSort(int arr[],n);

int bubbleSort(int arr[],n){
    int min = arr[0];
    for(int i=0;i<a;i++){
        if(arr[i+1]>arr[i]){
            arr[i]=arr[i+1];
        }
    }
    return arr;
}
