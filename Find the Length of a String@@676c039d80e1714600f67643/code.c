#include <stdio.h>

int main() {
    char ch[50];
    scanf("%s",&ch);
    int length=0;
    for(int i =0;arr[i]!='\0',i++){
        length++;
    }
    printf("%d",length);
    return 0;
}